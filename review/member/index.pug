html
    head
    body
        h1 User profile page
        h2 Nickname
        div=nickname

        h2 profile
        img(src=profileImageUrl)
        form(action=`/member/${useId}/profile` method='post' enctype="multipart/form-data")
            input(type="file" name="profile")
            button Upload file